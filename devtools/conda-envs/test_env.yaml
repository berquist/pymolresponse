---
name: test
channels:
  - conda-forge
dependencies:
  - cclib
  - coverage
  - pip
  - psi4
  - pytest-cov
  - pip:
      # out-of-date on conda-forge
      - daltools
      # out-of-date on conda-forge
      - pyscf
      # doesn't exist on conda-forge
      - pytest-ruff
      # doesn't exist on conda-forge
      - pytest-snob
  # documentation
  - sphinx
  - sphinx_rtd_theme
  - numpydoc
